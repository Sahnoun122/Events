// Page de réservation pour un événement
"use client";

export default function BookEventPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Réserver cet Événement</h1>
      {/* Formulaire de réservation avec vérification des règles */}
    </div>
  );
}