// Page de détail d'un événement pour participants
"use client";

export default function EventDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Détail de l'Événement</h1>
      {/* Informations complètes de l'événement + bouton de réservation */}
    </div>
  );
}