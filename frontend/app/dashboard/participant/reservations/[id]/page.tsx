// Page de détail d'une réservation spécifique
"use client";

export default function ReservationDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Détail de ma Réservation</h1>
      {/* Informations de la réservation + options selon statut */}
    </div>
  );
}